description: >
  Run node js script
parameters:
  run:
    type: string
    default: ""
    description: "Node js script to be run"
steps:
  - run:
      environment:
        SCRIPT: <<parameters.run>>
      name: Run NodeJS
      command: <<include(scripts/run.sh)>>
